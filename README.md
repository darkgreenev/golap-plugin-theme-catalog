# goLaPress Plugin Theme Catalog

This repository is the public distribution catalog for goLaPress plugins and themes.

It is not a source repository.

It should contain:

- catalog metadata
- plugin and theme release indexes
- packaged release artifacts
- checksums
- changelog files
- validation and publishing docs

Use the docs under `docs/` as the working contract for catalog structure and release publishing.
