# golap-commerce

Public release index for `golap-commerce`.

The source lives in the private `plugins/golap-commerce` repository. This folder only contains catalog-facing release metadata and package pointers.
