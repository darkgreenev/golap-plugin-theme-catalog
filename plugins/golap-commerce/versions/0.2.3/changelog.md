# goLaPress Commerce 0.2.3

- rebuilt catalog-ready Linux amd64 and arm64 release packages
- fixed the release packaging workflow so generated zip files are written from the plugin checkout reliably
- package metadata remains aligned with `plugin.json`
