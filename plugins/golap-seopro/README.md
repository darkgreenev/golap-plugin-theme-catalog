# golap-seopro

Public release index for `golap-seopro`.

The source lives in the private `plugins/golap-seopro` repository. This folder only contains catalog-facing release metadata and package pointers.
